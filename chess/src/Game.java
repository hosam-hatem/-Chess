import java.util.*; 

public class Game {
     
    public class Controller{
        Board newboard;
     //Methods
        public void CreateBoard(){newboard = new Board();}
        
        public boolean IsMoveAllowedforB(Piece piece,Square destination){
        /*
            if destination.state=='w' or 'f'
            piece.dest=destination;
            destination.id=piece.id
                if destinatiom.state == 'w'
                    destination.id
                   remove (destination.id) dy hya el hattshal
            return true
            else return false;
            */
        return true;
        }
        public void RemovePiece(char idtoremove){
         for(int i=0;i<16;i++)
         {
            // if (newboard.blackset.get(i));
            
    }
        }
        
    }
    
    
    
 }

